#ifndef MANHATTANDISTANCE_HPP
#define MANHATTANDISTANCE_HPP

#include <utility>
using std::pair;

double manhattanDist(class Point *, class Point *);
double manhattanDist(pair <double, double>,pair <double, double>);

#endif