#ifndef _BRUTE_FORCE_
#define _BRUTE_FORCE_

#include <vector>
using std::vector;

#include "point.hpp"

using namespace std;

int bruteForce(vector<class Point *> *, vector<class Point *> *);

#endif